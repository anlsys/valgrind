%!TEX root =  ../article.tex
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Introduction}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% CONTEXT

\emph{Determinacy races} are concurrent programming hazards occurring when two accesses on the same memory address are not ordered, and at least one is writing~\cite{determinator}.
The presence of determinacy races in a program can make its output vary given the same input, which may hint at the presence of correctness errors.
With the ever-growing intra-node parallelism of supercomputers, porting simulation codes while ensuring parallel execution correctness is challenging.
Parallel programming using tasks and fine-grain synchronizations (dependencies) allows the use of hardware' asynchronous capabilities.
However, such asynchronous parallel programming is error-prone in term of synchronizations, which can lead to an incorrect order of execution - and, in the end, to an erroneous simulation.
%\george{Maybe explain briefly why it's easier to be missing a synchronization in tasking systems? I think it is true, but maybe not obvious to the reader, and I think this is important for the strength of the paper.}
%\romain{I agree}

%\george{We switch between determinacy race and data race. Should probably clarify if you mean something differentor subtly different by data race, but I think they can be used synonymously.}
%\romain{yes - i don't think 'determinacy race' and 'data race' differences are clearly stated somewhere... I tend to prefer the term 'determinacy race' though}
To help prevent unintended determinacy races, a popular approach is to develop tools to detect them.
While many such tools have already been developed, each has drawbacks, which we summarize briefly here.
LLOV~\cite{LLOV} is a static data-race checker, meaning it detects the presence or absence of errors at compile-time, hence with a limited vision of the program.
Archer~\cite{archer} and TaskSanitizer~\cite{Matar18} instruments at compile-time to perform run-time analysis falling back to LLVM' ThreadSanitizer~\cite{thread-sanitizer-llvm}.
Motivations and results of Archer~\cite{archer} showed its effectiveness for \emph{debugging} applications, even at large scale.
However, as suggested by E. Dijkstra~\cite{EWD303}, \emph{"debugging is an inadequate means for ensuring programs correctness and that we must prove the correctness of programs"}.
In particular, both tools are subject to \emph{false negative}: it may report no data race in an erroneous program. 
One source of false negatives comes from non-instrumented code - for instance, in vendor-specific dynamic libraries which source code may not be visible at compile-time.
ROMP~\cite{ROMP} is a static binary instrumentation tool relying on Dyninst~\cite{dyninstr} for checking OpenMP program's correctness.
It tackles Archer/TaskSanitizer's limitation by recompiling and instrumenting \emph{binary programs}.
However, ROMP' main drawbacks are its poor error reporting and that it only supports OpenMP semantics.

This paper explores the use of heavyweight Dynamic Binary Instrumentation (DBI) towards (1) no false negatives and (2) meaningful determinacy race reports.
Our contributions are:
\begin{itemize}
	\item	the introduction of Taskgrind: a Valgrind tool for parallel programming model memory accesses analysis - with work-in-progress support for OpenMP and Cilk.
	\item 	a brief survey on pitfalls related to heavyweight DBI in parallel programs,
	\item 	the implementation in Taskgrind of a determinacy race analysis, and its evaluation with a comparison on state-of-the-art tools.
\end{itemize}

The paper is organized as follows.
\sect{sec:background} provides relevant background.
\sect{sec:taskgrind} introduces the design of Taskgrind and the determinacy race detector implementation.
\sect{sec:pitfalls} presents a few pitfalls on parallel programs heavyweight DBI, and how Taskgrind workarounds them.
\sect{sec:eval} evaluates Taskgrind overheads and its determinacy race detection analysis.
Finally, \sect{sec:relworks} review the literature, and we conclude in \sect{sec:conclusion}.
